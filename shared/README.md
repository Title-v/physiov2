# PhysioAI Shared Browser Modules

Route-neutral shared imports for patient and therapist web surfaces.

Browser-safe modules live under `ai/`, `core/`, `assets/`, `models/`, and
`vendor/`. Server/API helpers live under `api/` and are not served by
`/shared/*`.
